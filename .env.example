# Firebase (ver firebase-applet-config.json para valores del proyecto)
# No commitear secretos reales. Copiar a .env si se usa EAS Secrets.
EXPO_PUBLIC_FIREBASE_PROJECT_ID=gen-lang-client-0923763848

# HU-04 — Mapas offline Protomaps (sin Firebase Storage)
# Base HTTPS donde subes los .pmtiles extraídos con scripts/extract-route-pmtiles.mjs
# La app descarga: {CDN_BASE}/{routeId}/basemap.pmtiles
# EXPO_PUBLIC_PMTILES_CDN_BASE=https://tu-cdn.example.com/pmtiles

# Solo para el script extract-route-pmtiles.mjs (no hace falta en la app)
# PROTOMAPS_PLANET_URL=https://build.protomaps.com/20251229.pmtiles
# PROTOMAPS_MAX_ZOOM=16
