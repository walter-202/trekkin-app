# HU-XX: <título> — <emoji % real>

- **Dueño:** <nombre> · **Rama:** <rama>
- **Rol:** <rol>.
- **Narrativa:** como <rol> quiero <qué> para <por qué>.
- **Figura oficial:** `docs/USER_STORIES.md` § HU-XX.

## DoD (trazable a criterios)

1. `HU-XX C1` <criterio> — evidencia `ruta:línea`.
2. `HU-XX C2` <criterio> — evidencia `ruta:línea`.

## Brecha

- Qué falta y por qué (bloqueador mapa/offline/permiso/etc.).

## Archivos tocados / a tocar

- `ruta:línea` — qué y por qué.

## Siguiente paso

- Validación pendiente (`npm run lint`, `npm test`, Expo Go del flujo).
